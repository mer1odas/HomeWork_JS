// getComputedStyle(elem).width - выдает ширину вместе с скроллом, а elem.clientWidth без
// getComputedStyle(elem).width может вернуть auto, а не число
// elem.clientWidth ширина вместе с падингном а getComputedStyle(elem).width только ширину